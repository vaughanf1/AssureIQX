---
phase: 01-scaffold-and-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - requirements.txt
  - README.md
  - src/__init__.py
  - src/data/__init__.py
  - src/models/__init__.py
  - src/evaluation/__init__.py
  - src/explainability/__init__.py
  - src/utils/__init__.py
  - data/splits/.gitkeep
  - docs/.gitkeep
  - notebooks/.gitkeep
  - tests/.gitkeep
  - app/.gitkeep
autonomous: true

must_haves:
  truths:
    - "pip install -r requirements.txt completes without errors on Python 3.10-3.12"
    - "Project directory structure matches architecture spec with __init__.py files in every src/ subdirectory"
    - "README.md contains setup instructions, project structure, and available make targets"
    - ".gitignore prevents data_raw/, checkpoints/, and model weight files from being committed"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned Python dependencies"
      contains: "torch==2.6.0"
    - path: ".gitignore"
      provides: "Git exclusion rules for ML project"
      contains: "data_raw/"
    - path: "README.md"
      provides: "Setup instructions and project overview"
      min_lines: 50
    - path: "src/__init__.py"
      provides: "Root package marker"
    - path: "src/data/__init__.py"
      provides: "Data subpackage marker"
    - path: "src/models/__init__.py"
      provides: "Models subpackage marker"
    - path: "src/evaluation/__init__.py"
      provides: "Evaluation subpackage marker"
    - path: "src/explainability/__init__.py"
      provides: "Explainability subpackage marker"
    - path: "src/utils/__init__.py"
      provides: "Utils subpackage marker"
  key_links:
    - from: "requirements.txt"
      to: "torch + torchvision"
      via: "matched version pair"
      pattern: "torch==2\\.6\\.0.*torchvision==0\\.21\\.0"
---

<objective>
Create the complete project directory structure, .gitignore, pinned requirements.txt, and initial README.md for the AssureXRay bone tumor classification PoC.

Purpose: Establish the empty-but-correct project skeleton so that all subsequent phases have a clean structure to write into. After this plan, `pip install -r requirements.txt` works and the directory tree matches the architecture spec.

Output: Directory tree with __init__.py files, .gitignore, requirements.txt, README.md
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-and-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project directory structure and .gitignore</name>
  <files>
    .gitignore
    src/__init__.py
    src/data/__init__.py
    src/data/dataset.py
    src/data/transforms.py
    src/data/split_utils.py
    src/models/__init__.py
    src/models/classifier.py
    src/models/factory.py
    src/evaluation/__init__.py
    src/evaluation/metrics.py
    src/evaluation/visualization.py
    src/evaluation/bootstrap.py
    src/explainability/__init__.py
    src/explainability/gradcam.py
    src/utils/__init__.py
    configs/.gitkeep
    scripts/.gitkeep
    data/splits/.gitkeep
    docs/.gitkeep
    notebooks/.gitkeep
    tests/.gitkeep
    app/.gitkeep
    checkpoints/.gitkeep
    results/.gitkeep
    logs/.gitkeep
  </files>
  <action>
    Create the full directory structure matching the architecture spec. Every directory under src/ gets an __init__.py file (can be empty). Non-src directories that need to exist but start empty get a .gitkeep file.

    Create placeholder Python modules in src/ subdirectories. Each placeholder module should contain ONLY a module docstring describing what it will contain and which phase implements it. Example:
    ```python
    """PyTorch dataset class for BTXRD bone tumor radiographs.

    Implemented in Phase 3.
    """
    ```

    Do NOT add any imports, classes, or functions to placeholder modules -- they are documentation-only stubs.

    Create .gitignore using the complete template from 01-RESEARCH.md. It must include:
    - Standard Python patterns (__pycache__/, *.py[cod], *.egg-info/, etc.)
    - Virtual environment patterns (.venv/, venv/, env/)
    - IDE patterns (.vscode/, .idea/, *.swp, .DS_Store)
    - Jupyter patterns (.ipynb_checkpoints/)
    - ML-specific patterns (data_raw/, checkpoints/, *.pt, *.pth, *.onnx, *.pkl)
    - Results patterns (results/gradcam/*.png, results/*/training_log.csv)
    - Logs (logs/, *.log)
    - Secrets (.env, .env.*)

    Directory tree to create:
    ```
    AssureXRay/
    |-- configs/
    |-- scripts/
    |-- src/
    |   |-- __init__.py
    |   |-- data/
    |   |   |-- __init__.py
    |   |   |-- dataset.py          (placeholder)
    |   |   |-- transforms.py       (placeholder)
    |   |   |-- split_utils.py      (placeholder)
    |   |-- models/
    |   |   |-- __init__.py
    |   |   |-- classifier.py       (placeholder)
    |   |   |-- factory.py          (placeholder)
    |   |-- evaluation/
    |   |   |-- __init__.py
    |   |   |-- metrics.py          (placeholder)
    |   |   |-- visualization.py    (placeholder)
    |   |   |-- bootstrap.py        (placeholder)
    |   |-- explainability/
    |   |   |-- __init__.py
    |   |   |-- gradcam.py          (placeholder)
    |   |-- utils/
    |       |-- __init__.py
    |-- app/
    |-- data/
    |   |-- splits/
    |-- checkpoints/
    |-- results/
    |-- docs/
    |-- tests/
    |-- notebooks/
    |-- logs/
    ```
  </action>
  <verify>
    Run: `find src -name "*.py" -type f | sort` and confirm all __init__.py and placeholder module files exist.
    Run: `find . -name ".gitkeep" | sort` and confirm all empty directories have .gitkeep files.
    Run: `cat .gitignore | grep "data_raw/"` and confirm ML-specific patterns are present.
  </verify>
  <done>
    All directories exist. Every src/ subdirectory has __init__.py. Placeholder modules have docstrings only. .gitignore covers Python, IDE, ML, and secrets patterns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create requirements.txt and README.md</name>
  <files>
    requirements.txt
    README.md
  </files>
  <action>
    Create requirements.txt with exact version pins from 01-RESEARCH.md. Include the header comment block documenting Python version (>=3.10, <3.13, 3.12 recommended), GPU vs CPU install instructions, and section comments grouping dependencies logically.

    The exact content for requirements.txt (use verbatim from research):
    ```
    # AssureXRay Dependencies
    # Python >=3.10, <3.13 (3.12 recommended)
    #
    # For GPU (CUDA 12.1):
    #   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
    #
    # For CPU only (Mac/development):
    #   pip install -r requirements.txt

    # Core ML framework (must be a matched pair)
    torch==2.6.0
    torchvision==0.21.0

    # Model zoo
    timm==1.0.15

    # Augmentation (MIT-licensed archived version -- stable, no active maintenance)
    albumentations==2.0.8

    # Explainability
    grad-cam==1.5.5

    # Evaluation
    scikit-learn==1.7.2
    scipy==1.15.2

    # Data handling
    pandas==2.2.3
    numpy==2.2.3
    Pillow==11.1.0

    # Visualization
    matplotlib==3.10.0
    seaborn==0.13.2

    # Utilities
    tqdm==4.67.1
    pyyaml==6.0.2
    ```

    Create README.md with:
    1. Project title and one-line description
    2. Overview section (what AssureXRay does, dataset, approach)
    3. Setup section:
       - Prerequisites (Python 3.10-3.12, pip)
       - Clone repo
       - Create virtual environment
       - Install dependencies (CPU and GPU variants)
    4. Project Structure section showing the directory tree
    5. Available Commands section (referencing `make` targets -- download, audit, split, train, evaluate, gradcam, infer, report, demo, all)
    6. Configuration section (reference configs/default.yaml)
    7. License / Disclaimer section (BTXRD is CC BY-NC-ND 4.0, not for clinical use)

    README should be practical and concise -- no marketing language. Use code blocks for commands. Target ~80-120 lines.
  </action>
  <verify>
    Run: `pip install -r requirements.txt --dry-run 2>&1 | head -5` to confirm pip can resolve all dependencies (dry-run does not install).
    If dry-run is not supported by the pip version, run: `pip install -r requirements.txt` in a subprocess and confirm exit code 0.
    Run: `wc -l README.md` and confirm at least 50 lines.
    Run: `grep -c "torch==2.6.0" requirements.txt` returns 1.
    Run: `grep -c "torchvision==0.21.0" requirements.txt` returns 1.
  </verify>
  <done>
    requirements.txt has all 15 pinned dependencies with correct version pairs. README.md has setup instructions, project structure, available commands, and configuration reference. pip can parse requirements.txt without errors.
  </done>
</task>

</tasks>

<verification>
1. `find src -name "__init__.py" | wc -l` returns 6 (src, data, models, evaluation, explainability, utils)
2. `cat .gitignore | grep -c "data_raw\|checkpoints\|\.pt\|\.pth"` returns at least 4
3. `grep "torch==2.6.0" requirements.txt` succeeds
4. `grep "torchvision==0.21.0" requirements.txt` succeeds
5. `test -f README.md && echo "EXISTS"` prints EXISTS
6. All src/ placeholder modules contain docstrings only (no import statements, no class/function definitions)
</verification>

<success_criteria>
- Project directory tree matches architecture spec exactly
- Every src/ subdirectory has __init__.py
- .gitignore prevents data_raw/, checkpoints/, and model files from being tracked
- requirements.txt has all 15 dependencies with exact version pins
- torch and torchvision are a matched pair (2.6.0 / 0.21.0)
- README.md documents setup, structure, and available commands
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-and-infrastructure/01-01-SUMMARY.md`
</output>
